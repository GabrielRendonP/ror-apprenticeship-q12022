<%= form_with(model: poke_type) do |form| %>
  <% if poke_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poke_type.errors.count, "error") %> prohibited this poke_type from being saved:</h2>

      <ul>
        <% poke_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
